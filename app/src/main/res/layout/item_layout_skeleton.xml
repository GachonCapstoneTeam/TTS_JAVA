<com.facebook.shimmer.ShimmerFrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/shimmer_item_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/background_color">

<androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/skeleton_item_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:background="@color/white"
    android:layout_marginBottom="12dp">

    <TextView
        android:id="@+id/skeleton_item_title"
        android:layout_width="100sp"
        android:layout_height="22sp"
        android:background="@drawable/round_box"
        android:backgroundTint="@color/gray_dark"
        android:paddingBottom="5dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginLeft="10dp"/>

    <TextView
        android:id="@+id/skeleton_item_name"
        android:layout_width="50sp"
        android:layout_height="20sp"
        android:background="@drawable/round_box"
        android:backgroundTint="@color/gray_dark"
        android:layout_marginTop="5dp"
        app:layout_constraintTop_toBottomOf="@id/skeleton_item_title"
        app:layout_constraintStart_toStartOf="@id/skeleton_item_title" />

    <TextView
        android:id="@+id/skeleton_item_date"
        android:layout_width="80sp"
        android:layout_height="20sp"
        android:background="@drawable/round_box"
        android:backgroundTint="@color/gray_dark"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/skeleton_item_name"
        android:layout_marginEnd="30dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
</com.facebook.shimmer.ShimmerFrameLayout>